{
    "link": "{{ site_url }}/{{ post.path }}.html",
    "title": "{{ post.title }}",
    "date": "{{ post.date | date: "%a, %d %b %Y %H:%M:%S %z" }}",
    {%- unless post.excerpt == blank -%}"excerpt": "{{post.excerpt}}",{% endunless %}
    {%- unless post.content == blank -%}"content": "{{post.content | strip_newlines | escape}}",{% endunless %}
    {%- unless post.image == blank -%}"imageUrl": "{{post.image.url}}",{% endunless %}
    {%- unless post.video == blank -%}"videoUrl": "{{post.video.url}}",{% endunless %}
    {%- unless post.audio == blank -%}"audioUrl": "{{post.audio.url}}",{% endunless %}
    {%- unless post.link == blank -%}"linkUrl": "{{post.link}}",{% endunless %}
    "index": {{ forloop.index }},
    "tags": [
        {%- assign tag_list = post.tags | split: "," -%}
        {%- for tag in tag_list -%}
            {%- assign trimmed_tag = tag | strip -%}
            "{{tag}}"{% unless forloop.last %},{% endunless %}
        {%- endfor -%}
    ]
}
