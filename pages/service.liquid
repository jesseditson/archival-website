{% layout 'theme' title: "Service" %}
{% include 'inner-nav' %}


  <main class="main inner">

  <div id="mobile-nav">
    {% include "primary-nav" %}
  </div>

  <section class="body-section container breadcrumb-nav">
    <a href="/" class="back-to-home">&larr; Return home</a>
  </section>

  <!-- Service Detail -->
  <section id="service-detail" class="body-section container">
    <h2 class="heading">{{ service.name }}</h2>
    <div class="service-body">
      {{ service.body }}
    </div>

    <span class="service-price-large">{{ service.price }}</span>
    <a href="/#contact" class="primary-cta-large">Get more information</a>
  </section>
</main>
